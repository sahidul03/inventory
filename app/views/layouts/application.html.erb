<!DOCTYPE html>
<html>
<head>

  <meta charset="utf-8" />
  <title> Restaurant </title>
  <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" name="viewport" />
  <meta content="" name="description" />
  <meta content="" name="author" />

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href="/css/fontawesome/css/font-awesome.css" rel="stylesheet" type="text/css"/>
  <link href="/css/bootstrap/css/bootstrap.min.css" rel="stylesheet" type="text/css"/>
  <link href="/css/bootstrap-colorpicker/css/colorpicker.css" rel="stylesheet" type="text/css"/>
  <link href="/css/bootstrap-datepicker/css/datepicker3.css" rel="stylesheet" type="text/css"/>
</head>
<body class="page-header-fixed">
    <div class="overlay"></div>
    <% if user_signed_in? %>
        <%= render 'layouts/extra_options_for_profile' %>
        <%= render 'layouts/search_form' %>
    <% end %>
    <main class="page-content content-wrap">
      <% if user_signed_in? %>
          <%= render 'layouts/header_admin' %>
          <%= render 'layouts/left_side_bar_admin_new' %>
      <% end %>
    <div class="page-inner">
    <div id="main-wrapper">
      <!-- begin #content -->
      <% if user_signed_in? %>
            <%= render 'layouts/alert_messages' %>
            <%= yield %>
      <% else %>
          <% unless params[:controller]=='devise/sessions' %>
              <%= render 'layouts/alert_messages' %>
          <% end %>
          <%= yield %>
      <% end %>

      <!-- end #content -->
    </div><!-- Main Wrapper -->
      <% if user_signed_in? %>
          <div class="page-footer">
            <%= render 'layouts/footer_admin' %>
          </div>
      <% end %>

    </div><!-- Page Inner -->
    </div>

    </main><!-- Page Content -->
    <% if user_signed_in? %>
        <%= render 'layouts/extra_options' %>
    <% end %>
    <div class="cd-overlay"></div>
    <script src="/css/bootstrap/js/bootstrap.min.js"></script>
    <script src="/css/bootstrap-datepicker/js/bootstrap-datepicker.js"></script>
    <script src="/css/bootstrap-colorpicker/js/bootstrap-colorpicker.js"></script>
</body>
</html>
