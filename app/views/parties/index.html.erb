<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-10">
    <h2>List of Parties</h2>
    <ol class="breadcrumb">
      <li>
        <%= link_to 'Home', root_path, data: { no_turbolink: true } %>
      </li>
      <li>
        <%= link_to 'Create new party', new_party_path, data: { no_turbolink: true } %>
      </li>
    </ol>
  </div>
  <div class="col-lg-2">

  </div>
</div>


<!-- begin page #content -->

<!-- begin row -->
<div class="row border-bottom white-bg dashboard-header">

  <!-- begin col-12 -->
  <div class="col-md-12">
    <!-- begin panel -->
    <div class="panel panel-inverse">
      <div class="panel-heading">

        <h3 class="">All parties</h3>
      </div>
      <!--<div class="alert alert-info fade in">-->
      <!--<button type="button" class="close" data-dismiss="alert">-->
      <!--<span aria-hidden="true">&times;</span>-->
      <!--</button>-->
      <!--TableTools is a plug-in for the DataTables HTML table enhancer, which adds a highly customisable button toolbar to a DataTable.-->
      <!--</div>-->
      <div class="panel-body">
        <div class="table-responsive">
          <% if @parties.any? %>
          <table id="data-table" class="table table-striped table-bordered nowrap" width="100%">
            <thead>
            <tr>
              <th>Date</th>
              <th>Party Name</th>
              <th class="text-center">Status</th>
              <th class="">Creator</th>
              <th class="text-center">Actions</th>
            </tr>
            </thead>
            <tbody>

                <% @parties.each do |party| %>
                    <tr>
                      <td>
                        <%= party.created_at.strftime("%d-%m-%Y") %>
                      </td>
                      <td>
                        <%= link_to party.name, party_path(party), data: { no_turbolink: true } %>
                      </td>
                      <td class="text-center">
                        <span class="label label-<%= party.isActive ? 'success' : 'danger' %> "><%= party.isActive ? 'Active' : 'Inactive' %></span>
                      </td>
                      <td>
                        <%= link_to party.user.email, user_information_path(party.user.user_information), data: { no_turbolink: true } %>
                      </td>
                      <td class="text-center">

                        <%= link_to edit_party_path(party), data: { no_turbolink: true }, :class=>"btn btn-info btn-xs" do %>
                            Edit
                            <i class="fa fa-edit"></i>
                        <% end %>

                      </td>
                    </tr>
                <% end %>


            </tbody>
          </table>
          <% else %>
          <h4>There is no party, please <%= link_to 'create new party', new_party_path, data: { no_turblink: true } %></h4>
          <% end %>

        </div>
      </div>
    </div>
    <!-- end panel -->
  </div>
  <!-- end col-10 -->
</div>
<!-- end row -->

<!-- end page #content -->




<script>

</script>