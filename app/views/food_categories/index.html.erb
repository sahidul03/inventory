<!-- begin page #content -->

<!-- begin row -->
<div class="row border-bottom white-bg dashboard-header">

<!-- begin col-12 -->
<div class="col-md-12">
<!-- begin panel -->
<div class="panel panel-inverse">
<div class="panel-heading">

  <h3 class="">All food categories</h3>
</div>
<!--<div class="alert alert-info fade in">-->
  <!--<button type="button" class="close" data-dismiss="alert">-->
    <!--<span aria-hidden="true">&times;</span>-->
  <!--</button>-->
  <!--TableTools is a plug-in for the DataTables HTML table enhancer, which adds a highly customisable button toolbar to a DataTable.-->
<!--</div>-->
<div class="panel-body">
<div class="table-responsive">

<table id="data-table" class="table table-striped table-bordered nowrap" width="100%">
<thead>
<tr>
  <th>Date</th>
  <th>Name</th>
  <th class="text-center">Status</th>
  <th class="">Creator</th>
  <th class="text-center">Actions</th>
</tr>
</thead>
<tbody>
<% if @food_categories.any? %>
    <% @food_categories.each do |category| %>
        <tr>
          <td>
            <%= category.created_at.strftime("%d-%m-%Y") %>
          </td>
          <td>
            <%= link_to category.name, food_category_path(category), data: { no_turbolink: true } %>
          </td>
          <td class="text-center">
            <span class="label label-<%= category.isActive ? 'success' : 'danger' %> "><%= category.isActive ? 'Active' : 'Inactive' %></span>
          </td>
          <td>
            <%= link_to category.user.email, user_information_path(category.user.user_information), data: { no_turbolink: true } %>
          </td>
          <td class="text-center">

            <%= link_to edit_food_category_path(category), data: { no_turbolink: true }, :class=>"btn btn-info btn-xs" do %>
                 Edit
                <i class="fa fa-edit"></i>
            <% end %>

          </td>
        </tr>
    <% end %>
<% end %>


</tbody>
</table>
</div>
</div>
</div>
<!-- end panel -->
</div>
<!-- end col-10 -->
</div>
<!-- end row -->

<!-- end page #content -->




<script>
    $(document).ready(function() {
        TableManageTableTools.init();
    });

</script>